spring:
  application:
    name: MentalGrowhDiary
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true

  datasource:
    url: jdbc:mariadb://localhost:3306/mental
    username: root
    password: zerobase
    driver-class-name: org.mariadb.jdbc.Driver